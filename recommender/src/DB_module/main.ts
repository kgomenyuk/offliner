import { User } from "../recomendation_system/main";
import { TRUE } from "node-sass";

// eslint-disable-next-line @typescript-eslint/no-empty-function
function getGoods(params: Record<string, any>): JSON{
    return;
}